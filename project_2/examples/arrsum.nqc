@DEFINE MAX 5

I64 sumArray( REF I64 arr, I64 count )
BEGIN
	I64 i;
	i:=0;
	UNTIL ( i >= count ) BEGIN
		sumArray:=sumArray+arr[i]; /**/
		i:=i+1;
	END
END

I64 MAIN( )
BEGIN
	I64 res;
	I64 arr[MAX];
	arr[2]:=3;
	arr[2]:=arr[2]+2;

	res:=sumArray(arr,5);
	WRITEI(res);

	MAIN:=0;
END
