I64 factorial( I64 num )
BEGIN
	IF ( num <= 1 ) BEGIN factorial := 1; END
	ELSE BEGIN factorial := num * factorial( (num - 1) ); END
END

I64 MAIN( )
BEGIN
	I64 flag_err;
	I64 num;

	flag_err := 0;
	num := 5;

	num := factorial( num );

	writei( num );
	writes( "" );

	MAIN := flag_err;
END

