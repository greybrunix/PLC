INT main(INT argc)
{
    INT flag_err = 0;
    INT x = 20;
    INT y = 30;
    INT p[10] = {1,2,3,4,5};
    
    IF (!cond)
    {

    }
    ELSE
    {

    }
    y = p[3];
    x = y + !(x >> 1);

    RETURN flag_err;
}
